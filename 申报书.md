# 项目申报书：pinyin — MoonBit 拼音转换库

## 一、项目概述

**pinyin**（模块名 `walkzzz/pinyin`）是一个支持将汉字转换成拼音的 MoonBit 三方库，移植自 Cangjie 库 [pinyin4cj](https://gitcode.com/pinyin4cj/pinyin4cj)。支持词/句转拼音、简繁互转、多音字、通用拼音、自定义字典，跨 wasm/js/native 三后端。

- 仓库链接：https://github.com/toadium/pinyin
- mooncakes 模块：`walkzzz/pinyin`

## 二、项目现有基础

移植工作已全部完成（R1-R7），当前状态：

- **核心功能**：PinyinHelper（8 个公开 API）+ ChineseHelper（6 个公开 API）+ PinyinFormat（4 种格式）
- **字典数据**：4 张字典内嵌为 MoonBit 字面量（单字拼音 20903 条、词组拼音 843 条、繁简映射 2533 条、通用拼音 82 条）
- **测试**：60 个内联测试全部通过（`moon test` 60/60）
- **构建**：`moon check` 通过，三后端兼容
- **示例**：`src/examples/` 含 4 个可运行示例，README 含 10 个代码示例
- **文档**：README.md（237 行）、CHANGELOG.md、doc/feature_api.md、申报书.md
- **工程**：src/ 目录布局、.gitignore、LICENSE（MIT）、GitHub Actions CI

## 三、本次计划开发/新增内容

移植已完成，本次重点为参赛验收准备：

1. ✅ 补充 LICENSE 文件（MIT，保留原版权）
2. ✅ 配置 GitHub Actions CI（check + test + fmt）
3. ✅ 设置 moon.mod repository 字段
4. ✅ 编写申报书与移植说明
5. ✅ 模块名改为 walkzzz/pinyin（匹配 mooncakes 用户名）
6. 📋 发布至 mooncakes.io（`moon publish`）

## 四、技术路线

- **字典内嵌**：构建期内嵌为 MoonBit 字面量，无运行时 IO
- **src/ 布局**：源码、数据、示例分层组织，对齐源库结构
- **三后端兼容**：wasm-gc / js / native 均通过 `moon check --target all`
- **命名映射**：camelCase → snake_case（如 `convertToPinyinString` → `convert_to_pinyin_string`）
- **错误处理**：使用 `raise PinyinError`（非 Result 类型）
- **声调转换**：内部模块 tone_conversion 处理带调元音与声调数字互转

## 五、预期目标

- 通过 MoonBit 黑客松项目验收
- 发布至 mooncakes.io，服务 MoonBit 生态
- 提供完整可用的拼音转换功能，填补 MoonBit 生态空白

## 六、测试与文档

- **测试**：60 个内联测试覆盖正常/边界/异常场景（easy/mid/difficult 三级）
- **文档**：README.md 含完整 API 表、10 个使用示例、字典说明、构建说明、移植说明
- **示例**：src/examples/ 含 4 个独立可运行示例
- **CI**：GitHub Actions 自动运行 check + test + fmt
- **变更日志**：CHANGELOG.md 记录 V0.1.0 和 V0.1.1 变更

## 七、移植说明

| 项 | 内容 |
|---|---|
| 原项目名称 | pinyin4cj |
| 原项目链接 | https://gitcode.com/pinyin4cj/pinyin4cj |
| 原项目语言 | Cangjie（仓颉） |
| 原项目许可证 | MIT |
| 移植范围 | pinyin_helper + chinese_helper 全部 API，完整移植 |
| 修改适配 | camelCase→snake_case、异常改用 raise、字典内嵌为字面量、src/ 布局 |
| 未支持功能 | 无（完整移植） |

## 八、代码规模说明

项目共 25119 行 MoonBit 代码，其中：
- 字典数据字面量约 24000 行（4 张字典）
- 核心逻辑约 1100 行（pinyin_helper + chinese_helper + tone_conversion + 类型定义）

超出 R5.1 参考范围（4000-10000 行）主要因字典数据占比，核心逻辑规模合理。
