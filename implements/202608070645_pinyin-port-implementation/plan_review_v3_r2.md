# 计划审查报告（v3 r2）

## 审查结果
APPROVED

## 发现

- **[轻微]** task_v3.md 第 10 行（"约 2556 条繁→简映射"）与第 11 行（"约 856 条词组拼音"）使用约数描述，而第 123-124 行验证契约使用精确值（"条目数 == 2556" / "条目数 == 856"）。任务描述与验证契约的措辞一致性可优化（统一为精确值或保留约数并注明精确值见验证契约），但不影响正确性——验证契约已明确要求精确断言，且第 125 行补充说明"若与上述预期不符，脚本应打印实际计数并断言失败，由编码 agent 核对源库后修正预期值（而非放宽断言）"，处置路径清晰。

- **[轻微]** task_v3.md 未明确要求生成产物（`data/*.mbt`）含文件头注释（如 `/// 自动生成，请勿手动修改`）。生成产物是纯数据字面量，注释可有可无，但考虑到产物入版本控制且由脚本生成，添加文件头注释有助于维护者识别"自动生成"属性。不影响正确性，编码 agent 可酌情添加。

- **[轻微]** task_v3.md 未明确要求生成脚本（`scripts/gen_pinyin_dict.py`）使用跨平台路径处理（如 `os.path.join` 或 `pathlib.Path`）。task_v3.md 第 8 行和第 162 行使用 Windows 路径分隔符 `\`，若脚本硬编码 `\` 将无法在非 Windows 平台运行。但本项目运行环境为 Windows，且脚本仅用于构建期一次性生成，不影响 MoonBit 产物的跨平台性。不影响正确性，编码 agent 可酌情使用跨平台路径处理。

## 上一轮驳回问题修订核验

| 上一轮问题 | 严重程度 | 修订位置 | 核验结论 |
|-----------|---------|---------|---------|
| 可见性修饰符描述自相矛盾（同时提供 `pub let` 与 `pub(self) let` 备选，错误声称 `pub(self)` 可被主包引用） | 严重 | task_v3.md:105-109 | 已删除 `pub(self) let` 备选与错误陈述，明确要求 `pub let`，给出 3 条理由（`pub(self)` 语义错误 / `pub let` 足矣 / wiki 确认），注明已查阅 wiki `language/packages.md:79-80`。**修订充分** |
| 生成脚本未明确要求 UTF-8 编码读写 | 一般 | task_v3.md:110 | 已补充"生成脚本编码要求（强制）"小节，明确所有文件读写显式指定 `encoding="utf-8"`，说明 Windows 平台默认编码非 UTF-8 及缺陷隐蔽性。**修订充分** |
| 生成脚本未要求确定性输出顺序 | 一般 | task_v3.md:111-114 | 已补充"生成脚本确定性输出要求（强制）"小节，明确按 key 排序输出（`chinese_dict` 按 Int key 升序，其余按 String key 字典序），确保多次运行字节级一致。**修订充分** |
| 完整性校验对部分字典使用约等于容差 | 轻微 | task_v3.md:120-125 | 已改为四张字典均精确条目数断言（严格相等，不使用约等于容差），并补充说明若实际计数与预期不符应断言失败由编码 agent 修正预期值。**修订充分** |

## 独立审查补充说明

本轮审查为独立进行，从以下角度逐一核查，未发现严重或一般问题：

1. **任务粒度**：4 张字典均为纯数据字面量（非复杂行为类型），紧密相关，合并为一个任务符合粒度约定。
2. **依赖关系**：R3 依赖 R1（项目骨架），不依赖 R2（基础类型）；后续 R4-R8 通过 R3 数据子包间接依赖。依赖链清晰，无循环依赖。
3. **可见性决策**：`pub let` 是最小充分可见性，数据子包常量是纯数据（`Map[Int,Int]` / `Map[String,String]`），无内部成员需公开，无需 `pub(all) let`。wiki 确认跨包 `@data` 引用正确性。
4. **转写规则**：§5.2 的 4 个子节描述清晰，16 进制码点字面量（`chinese_dict`）与字符串直接转写（其余三张）的区分明确。
5. **验证契约**：`moon check` 通过 + `unused_package` 警告预期（持续至 R4 消除）+ 四张字典精确条目数断言，契约完整。
6. **后续任务边界**：明确列出不创建的后续任务文件（`pinyin_dicts.mbt` / `tone_conversion.mbt` / `pinyin_helper.mbt` / `chinese_helper.mbt` / `pinyin_spec.mbt` / 测试文件 / `README.mbt.md` 填充），避免过度设计。
7. **plan.md 与 task_v3.md 一致性**：plan.md R3 RETRY 部分（第 74-84 行）的修订要点 1-4 与 task_v3.md 修订说明（第 181-188 行）一致，与 plan_review_v3_r1.md 修改要求（第 24-47 行）一致。
8. **源库保真度**：4 张字典的源库格式描述（第 60-99 行）与转写规则对齐，条目数预期值与源库行数对应关系清晰。
9. **用户偏好符合性**：文件名 snake_case（PREFERENCE_1）、类型名 PascalCase（PREFERENCE_14）、MoonBit 语言（PREFERENCE_19）、简体中文交互（PREFERENCE_20）、彻底根因分析（PREFERENCE_4，体现在 UTF-8 编码问题分析）、批量测试（PREFERENCE_8，本任务不执行 `moon test`，测试在后续任务中编写）均符合。